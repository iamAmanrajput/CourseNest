import React from "react";
import { RingLoader } from "react-spinners"; // Import RingLoader

const Spinner = () => {
  return (
    <div>
      <RingLoader color="#3498db" size={200} />
    </div>
  );
};

export default Spinner;
